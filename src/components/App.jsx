'use strict';

import React from 'react';
import Form from './Form.jsx';

export default React.createClass({
    displayName: 'App',

    render() {
        return (
            <div>
                <Form />
            </div>
        );
    }
});